name: Build Blender iOS - Fixed

on:
  workflow_dispatch:

concurrency:
  group: blender-ios-fixed-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Blender iOS
    runs-on: macos-15
    timeout-minutes: 180

    steps:
      # ============================================================
      # 1. CHECKOUT SALMAZOV
      # ============================================================

      - name: Checkout Salmazov ios-new
        uses: actions/checkout@v6
        with:
          repository: salmazov/blender-ios
          ref: ios-new
          fetch-depth: 1
          lfs: false
          submodules: false
          persist-credentials: false

      # ============================================================
      # 2. CHECKOUT EXACT SOURCE REVISION
      # ============================================================

      - name: Checkout exact Blender source
        run: |
          set -euxo pipefail

          EXPECTED="1e391616fea93b759ac5ad73cdbbbcb7f2fa08e6"

          git fetch \
            origin \
            "$EXPECTED" \
            --depth=1

          git checkout \
            --detach \
            FETCH_HEAD

          ACTUAL="$(git rev-parse HEAD)"

          echo "Expected: $EXPECTED"
          echo "Actual:   $ACTUAL"

          test "$ACTUAL" = "$EXPECTED"

          git log -1 --oneline

      # ============================================================
      # 3. ENVIRONMENT
      # ============================================================

      - name: Show environment
        run: |
          set -eux

          sudo xcode-select -s /Applications/Xcode.app

          echo "===== macOS ====="
          sw_vers

          echo
          echo "===== CPU ====="
          uname -m

          echo
          echo "===== Xcode ====="
          xcodebuild -version

          echo
          echo "===== iPhoneOS SDK ====="
          xcrun --sdk iphoneos --show-sdk-version
          xcrun --sdk iphoneos --show-sdk-path

          echo
          echo "===== Source ====="
          git rev-parse HEAD

          echo
          echo "===== Disk ====="
          df -h

      # ============================================================
      # 4. FREE DISK SPACE
      # ============================================================

      - name: Free disk space
        run: |
          set -eux

          sudo rm -rf /usr/local/lib/android || true
          sudo rm -rf /usr/share/dotnet || true
          sudo rm -rf ~/Library/Android || true
          sudo rm -rf /opt/ghc || true
          sudo rm -rf /usr/local/.ghcup || true
          sudo rm -rf /Library/Developer/CoreSimulator/Profiles || true

          sudo xcrun simctl delete all 2>/dev/null || true

          df -h

      # ============================================================
      # 5. INSTALL BUILD TOOLS
      # ============================================================

      - name: Install build tools
        run: |
          set -eux

          brew install cmake git-lfs

          git lfs install

          cmake --version
          git lfs version
          python3 --version

      # ============================================================
      # 6. FETCH startup.blend
      # ============================================================

      - name: Fetch startup.blend
        run: |
          set -euxo pipefail

          git remote add blender-lfs \
            https://projects.blender.org/blender/blender.git \
            2>/dev/null || true

          git remote set-url \
            blender-lfs \
            https://projects.blender.org/blender/blender.git

          git config lfs.remote.searchall true

          git lfs fetch \
            blender-lfs \
            --include="release/datafiles/startup.blend" \
            2>&1 | tee source-lfs.log

          git lfs checkout \
            release/datafiles/startup.blend

          test -f release/datafiles/startup.blend

          SIZE="$(
            stat -f '%z' \
              release/datafiles/startup.blend
          )"

          echo "startup.blend size: $SIZE"

          if [ "$SIZE" -lt 1024 ]; then
            echo "ERROR: startup.blend is still an LFS pointer"
            exit 1
          fi

          file release/datafiles/startup.blend

      # ============================================================
      # 7. DETERMINE EXACT LIBRARY REVISIONS
      # ============================================================

      - name: Determine library revisions
        id: libs
        run: |
          set -eux

          IOS_SHA="$(
            git rev-parse HEAD:lib/ios_arm64
          )"

          MAC_SHA="$(
            git rev-parse HEAD:lib/macos_arm64
          )"

          IOS_URL="$(
            git config \
              -f .gitmodules \
              --get submodule.lib/ios_arm64.url
          )"

          MAC_URL="$(
            git config \
              -f .gitmodules \
              --get submodule.lib/macos_arm64.url
          )"

          echo "ios_sha=$IOS_SHA" >> "$GITHUB_OUTPUT"
          echo "mac_sha=$MAC_SHA" >> "$GITHUB_OUTPUT"
          echo "ios_url=$IOS_URL" >> "$GITHUB_OUTPUT"
          echo "mac_url=$MAC_URL" >> "$GITHUB_OUTPUT"

          echo "===== iOS ====="
          echo "$IOS_URL"
          echo "$IOS_SHA"

          echo
          echo "===== macOS ====="
          echo "$MAC_URL"
          echo "$MAC_SHA"

      # ============================================================
      # 8. VERIFY EXACT KNOWN LIBRARY REVISIONS
      # ============================================================

      - name: Verify library revisions
        run: |
          set -eux

          EXPECTED_IOS="393201c7c8525941553f6a96e19b909d6b3bfc4f"
          EXPECTED_MAC="a3e20428fb0ab2231903608cdca90301e130dbfc"

          ACTUAL_IOS="${{ steps.libs.outputs.ios_sha }}"
          ACTUAL_MAC="${{ steps.libs.outputs.mac_sha }}"

          echo "iOS expected: $EXPECTED_IOS"
          echo "iOS actual:   $ACTUAL_IOS"

          echo
          echo "macOS expected: $EXPECTED_MAC"
          echo "macOS actual:   $ACTUAL_MAC"

          test "$ACTUAL_IOS" = "$EXPECTED_IOS"
          test "$ACTUAL_MAC" = "$EXPECTED_MAC"

      # ============================================================
      # 9. CACHE IOS LIBRARIES
      # ============================================================

      - name: Cache iOS libraries
        id: ios-cache
        uses: actions/cache@v5
        with:
          path: lib/ios_arm64
          key: blender-ios-arm64-${{ steps.libs.outputs.ios_sha }}-v3

      # ============================================================
      # 10. CACHE MACOS LIBRARIES
      # ============================================================

      - name: Cache macOS libraries
        id: mac-cache
        uses: actions/cache@v5
        with:
          path: lib/macos_arm64
          key: blender-macos-arm64-${{ steps.libs.outputs.mac_sha }}-v3

      # ============================================================
      # 11. FETCH IOS LIBRARIES
      # ============================================================

      - name: Fetch iOS libraries
        if: steps.ios-cache.outputs.cache-hit != 'true'
        env:
          GIT_LFS_SKIP_SMUDGE: "1"
        run: |
          set -euxo pipefail

          rm -rf lib/ios_arm64

          git clone \
            --filter=blob:none \
            --no-checkout \
            "${{ steps.libs.outputs.ios_url }}" \
            lib/ios_arm64

          git -C lib/ios_arm64 \
            fetch \
            --depth=1 \
            origin \
            "${{ steps.libs.outputs.ios_sha }}"

          git -C lib/ios_arm64 \
            checkout \
            --detach \
            FETCH_HEAD

          ACTUAL="$(
            git -C lib/ios_arm64 rev-parse HEAD
          )"

          EXPECTED="${{ steps.libs.outputs.ios_sha }}"

          echo "Expected: $EXPECTED"
          echo "Actual:   $ACTUAL"

          test "$ACTUAL" = "$EXPECTED"

          git -C lib/ios_arm64 \
            lfs install --local

          git -C lib/ios_arm64 \
            lfs pull \
            2>&1 | tee ios-lfs.log

          du -sh lib/ios_arm64

      # ============================================================
      # 12. FETCH MACOS LIBRARIES
      # ============================================================

      - name: Fetch macOS libraries
        if: steps.mac-cache.outputs.cache-hit != 'true'
        env:
          GIT_LFS_SKIP_SMUDGE: "1"
        run: |
          set -euxo pipefail

          rm -rf lib/macos_arm64

          git clone \
            --filter=blob:none \
            --no-checkout \
            "${{ steps.libs.outputs.mac_url }}" \
            lib/macos_arm64

          git -C lib/macos_arm64 \
            fetch \
            --depth=1 \
            origin \
            "${{ steps.libs.outputs.mac_sha }}"

          git -C lib/macos_arm64 \
            checkout \
            --detach \
            FETCH_HEAD

          ACTUAL="$(
            git -C lib/macos_arm64 rev-parse HEAD
          )"

          EXPECTED="${{ steps.libs.outputs.mac_sha }}"

          echo "Expected: $EXPECTED"
          echo "Actual:   $ACTUAL"

          test "$ACTUAL" = "$EXPECTED"

          git -C lib/macos_arm64 \
            lfs install --local

          git -C lib/macos_arm64 \
            lfs pull \
            2>&1 | tee macos-lfs.log

          du -sh lib/macos_arm64

      # ============================================================
      # 13. VERIFY LIBRARIES
      # ============================================================

      - name: Verify prebuilt libraries
        run: |
          set -eux

          IOS_ACTUAL="$(
            git -C lib/ios_arm64 rev-parse HEAD
          )"

          MAC_ACTUAL="$(
            git -C lib/macos_arm64 rev-parse HEAD
          )"

          test "$IOS_ACTUAL" = \
            "${{ steps.libs.outputs.ios_sha }}"

          test "$MAC_ACTUAL" = \
            "${{ steps.libs.outputs.mac_sha }}"

          test -f \
            lib/ios_arm64/python/lib/libpython3.11.a

          echo "===== iOS Python ====="

          ls -lh \
            lib/ios_arm64/python/lib/libpython3.11.a

          echo
          echo "===== Host Python ====="

          HOST_PYTHON="$(
            find \
              lib/macos_arm64/python/bin \
              -type f \
              -name 'python3.*' \
              -not -name '*config*' \
              -print -quit
          )"

          if [ -z "$HOST_PYTHON" ]; then
            echo "ERROR: macOS host Python missing"
            exit 1
          fi

          echo "$HOST_PYTHON"

          echo
          echo "===== Sizes ====="

          du -sh lib/ios_arm64
          du -sh lib/macos_arm64

      # ============================================================
      # 14. PATCH OLD FMT / BundleKey COMPATIBILITY
      #
      # IMPORTANT:
      #
      # node_geo_join_bundle.cc contains BundleKey.
      #
      # geometry_nodes_bundle.cc has TWO fmt::join(path, "/")
      # expressions, but the element type is not identical:
      #
      # one can contain StringRef
      # one can contain BundleKey
      #
      # The previous patch treated BOTH as BundleKey and therefore
      # attempted key.ustr() on a StringRef.
      #
      # This replacement uses an overloaded local helper that knows
      # how to append BOTH types.
      # ============================================================

      - name: Patch BundleKey formatting
        run: |
          set -euxo pipefail

          python3 <<'PY'
          from pathlib import Path


          # ========================================================
          # PATCH 1
          #
          # node_geo_join_bundle.cc
          #
          # overridden_keys contains BundleKey values.
          # ========================================================

          file1 = Path(
              "source/blender/nodes/geometry/nodes/"
              "node_geo_join_bundle.cc"
          )

          text1 = file1.read_text()

          needle1 = 'fmt::join(overridden_keys, ", ")'

          replacement1 = '''[&]() {
                std::string result;

                for (const auto &key : overridden_keys) {
                  if (!result.empty()) {
                    result += ", ";
                  }

                  const auto key_ref = key.ustr().ref();

                  result.append(
                      key_ref.data(),
                      key_ref.size());
                }

                return result;
              }()'''

          count1 = text1.count(needle1)

          print(
              "node_geo_join_bundle.cc occurrences:",
              count1,
          )

          if count1 != 1:
              raise SystemExit(
                  "ERROR: expected exactly 1 "
                  "fmt::join(overridden_keys, \", \")"
              )

          text1 = text1.replace(
              needle1,
              replacement1,
              1,
          )

          file1.write_text(text1)

          print("Patched:", file1)


          # ========================================================
          # PATCH 2
          #
          # geometry_nodes_bundle.cc
          #
          # There are TWO path joins.
          #
          # Crucially, path elements can be either:
          #
          #   blender::StringRef
          #   BundleKey
          #
          # So provide overloads for BOTH.
          # ========================================================

          file2 = Path(
              "source/blender/nodes/intern/"
              "geometry_nodes_bundle.cc"
          )

          text2 = file2.read_text()

          needle2 = 'fmt::join(path, "/")'

          replacement2 = '''[&]() {
                struct PathPartAppender {
                  static void append(
                      std::string &result,
                      const StringRef value)
                  {
                    result.append(
                        value.data(),
                        value.size());
                  }

                  static void append(
                      std::string &result,
                      const BundleKey &value)
                  {
                    const auto value_ref =
                        value.ustr().ref();

                    result.append(
                        value_ref.data(),
                        value_ref.size());
                  }
                };

                std::string result;

                for (const auto &key : path) {
                  if (!result.empty()) {
                    result += '/';
                  }

                  PathPartAppender::append(
                      result,
                      key);
                }

                return result;
              }()'''

          count2 = text2.count(needle2)

          print(
              "geometry_nodes_bundle.cc occurrences:",
              count2,
          )

          if count2 != 2:
              raise SystemExit(
                  "ERROR: expected exactly 2 "
                  "fmt::join(path, \"/\") expressions, "
                  f"found {count2}"
              )

          text2 = text2.replace(
              needle2,
              replacement2,
          )

          file2.write_text(text2)

          print(
              "Patched all",
              count2,
              "path joins in:",
              file2,
          )


          # ========================================================
          # VERIFY
          # ========================================================

          remaining1 = (
              file1.read_text().count(needle1)
          )

          remaining2 = (
              file2.read_text().count(needle2)
          )

          print()
          print(
              "Remaining overridden_keys joins:",
              remaining1,
          )

          print(
              "Remaining path joins:",
              remaining2,
          )

          if remaining1 != 0:
              raise SystemExit(
                  "ERROR: overridden_keys fmt::join remains"
              )

          if remaining2 != 0:
              raise SystemExit(
                  "ERROR: path fmt::join remains"
              )

          print()
          print(
              "BundleKey/StringRef compatibility "
              "patch successful."
          )
          PY

          echo
          echo "========================================"
          echo "PATCH DIFF"
          echo "========================================"

          git diff -- \
            source/blender/nodes/geometry/nodes/node_geo_join_bundle.cc \
            source/blender/nodes/intern/geometry_nodes_bundle.cc

          git diff -- \
            source/blender/nodes/geometry/nodes/node_geo_join_bundle.cc \
            source/blender/nodes/intern/geometry_nodes_bundle.cc \
            > compatibility.patch

          echo
          echo "===== Verify original joins are gone ====="

          if grep -RIn \
            'fmt::join(overridden_keys' \
            source/blender/nodes
          then
            echo "ERROR: overridden_keys fmt::join remains"
            exit 1
          fi

          if grep -n \
            'fmt::join(path, "/")' \
            source/blender/nodes/intern/geometry_nodes_bundle.cc
          then
            echo "ERROR: path fmt::join remains"
            exit 1
          fi

          echo
          echo "Patch verification passed."

      # ============================================================
      # 15. CONFIGURE
      #
      # Cycles remains OFF because it is a separate known failure.
      #
      # NO fmt replacement.
      # NO CPATH.
      # NO custom fmt headers.
      # ============================================================

      - name: Configure Blender
        run: |
          set -euxo pipefail

          rm -rf build_ios

          IOS_LIBDIR="$GITHUB_WORKSPACE/lib/ios_arm64"

          IOS_SDK_ROOT="$(
            xcrun \
              --sdk iphoneos \
              --show-sdk-path
          )"

          IOS_DEV_ROOT="$(
            dirname "$IOS_SDK_ROOT"
          )"

          echo "===== SDK ====="
          echo "$IOS_SDK_ROOT"

          echo
          echo "===== LIBDIR ====="
          echo "$IOS_LIBDIR"

          cmake \
            -S . \
            -B build_ios \
            -G Xcode \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DWITH_APPLE_CROSSPLATFORM=ON \
            -DAPPLE_TARGET_DEVICE=ios \
            -DWITH_CYCLES=OFF \
            -DCMAKE_FIND_ROOT_PATH="$IOS_DEV_ROOT;$(dirname "$IOS_DEV_ROOT");$IOS_LIBDIR" \
            2>&1 | tee configure.log

      # ============================================================
      # 16. VERIFY CONFIGURATION
      # ============================================================

      - name: Verify configuration
        run: |
          set -eux

          echo "===== Important CMake options ====="

          grep -E \
            'WITH_CYCLES:|WITH_PYTHON:|WITH_PYTHON_NUMPY:|WITH_AUDASPACE:|WITH_METAL_BACKEND:' \
            build_ios/CMakeCache.txt \
            || true

          CYCLES="$(
            grep '^WITH_CYCLES:BOOL=' \
              build_ios/CMakeCache.txt \
              | cut -d= -f2
          )"

          echo
          echo "WITH_CYCLES=$CYCLES"

          if [ "$CYCLES" != "OFF" ]; then
            echo "ERROR: Cycles is not OFF"
            exit 1
          fi

          PYTHON="$(
            grep '^WITH_PYTHON:BOOL=' \
              build_ios/CMakeCache.txt \
              | cut -d= -f2
          )"

          echo "WITH_PYTHON=$PYTHON"

          if [ "$PYTHON" != "ON" ]; then
            echo "ERROR: Python is not ON"
            exit 1
          fi

      # ============================================================
      # 17. BUILD BLENDER
      # ============================================================

      - name: Build Blender for physical iOS
        run: |
          set -o pipefail

          NCPU="$(
            sysctl -n hw.ncpu
          )"

          echo "========================================"
          echo "BUILD BLENDER"
          echo "========================================"

          echo "Jobs: $NCPU"

          set +e

          xcodebuild \
            -project build_ios/Blender.xcodeproj \
            -scheme blender \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -jobs "$NCPU" \
            ARCHS=arm64 \
            ONLY_ACTIVE_ARCH=YES \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build \
            > xcodebuild.log 2>&1

          STATUS=$?

          set -e

          if [ "$STATUS" -ne 0 ]; then

            echo
            echo "========================================"
            echo "BUILD FAILED"
            echo "========================================"

            echo
            echo "===== Compiler/linker errors ====="

            grep -n -C 25 \
              -E \
              '(^|[[:space:]])error:|fatal error:|static assertion failed|Undefined symbols|duplicate symbol|ld: |clang: error:' \
              xcodebuild.log \
              > compiler-errors.txt \
              || true

            cat \
              compiler-errors.txt \
              || true

            echo
            echo "===== fmt / Bundle diagnostics ====="

            grep -n -C 12 \
              -E \
              'fmt::|BundleKey|StringRef|fallback_formatter|formattable|OpenImageIO/detail/fmt' \
              xcodebuild.log \
              > fmt-errors.txt \
              || true

            cat \
              fmt-errors.txt \
              || true

            echo
            echo "===== Failed commands ====="

            grep -n -A 100 \
              'The following build commands failed:' \
              xcodebuild.log \
              > failed-commands.txt \
              || true

            cat \
              failed-commands.txt \
              || true

            echo
            echo "===== Last 500 lines ====="

            tail -500 \
              xcodebuild.log \
              || true

            exit "$STATUS"
          fi

          echo
          echo "========================================"
          echo "BUILD SUCCEEDED"
          echo "========================================"

          tail -120 \
            xcodebuild.log

      # ============================================================
      # 18. FIND Blender.app
      # ============================================================

      - name: Find Blender.app
        run: |
          set -eux

          APP="$(
            find \
              "$GITHUB_WORKSPACE/build_ios" \
              -type d \
              -name 'Blender.app' \
              -print -quit
          )"

          if [ -z "$APP" ] || [ ! -d "$APP" ]; then

            echo "ERROR: Blender.app not found"

            echo
            echo "===== All .app bundles ====="

            find \
              build_ios \
              -type d \
              -name '*.app' \
              -print \
              || true

            exit 1
          fi

          echo "APP=$APP" \
            >> "$GITHUB_ENV"

          echo
          echo "===== Blender.app ====="

          echo "$APP"

          du -sh "$APP"

      # ============================================================
      # 19. VERIFY APP
      # ============================================================

      - name: Verify Blender executable
        run: |
          set -eux

          test -f "$APP/Info.plist"

          EXEC_NAME="$(
            /usr/libexec/PlistBuddy \
              -c 'Print :CFBundleExecutable' \
              "$APP/Info.plist" \
              2>/dev/null \
              || true
          )"

          EXECUTABLE=""

          if [ -n "$EXEC_NAME" ] && \
             [ -f "$APP/$EXEC_NAME" ]
          then

            EXECUTABLE="$APP/$EXEC_NAME"

          elif [ -f "$APP/Blender" ]
          then

            EXEC_NAME="Blender"
            EXECUTABLE="$APP/Blender"

          else

            echo "ERROR: Blender executable not found"

            find \
              "$APP" \
              -maxdepth 1 \
              -type f \
              -print

            exit 1
          fi

          echo "EXEC_NAME=$EXEC_NAME" \
            >> "$GITHUB_ENV"

          echo "EXECUTABLE=$EXECUTABLE" \
            >> "$GITHUB_ENV"

          echo "===== Executable ====="
          echo "$EXECUTABLE"

          echo
          echo "===== File ====="
          file "$EXECUTABLE"

          echo
          echo "===== Architecture ====="
          lipo -info "$EXECUTABLE" || true

      # ============================================================
      # 20. INSPECT APP
      # ============================================================

      - name: Inspect Blender.app
        run: |
          set -eux

          echo "===== App size ====="
          du -sh "$APP"

          echo
          echo "===== Top-level contents ====="

          find \
            "$APP" \
            -maxdepth 2 \
            -print \
            | head -150

      # ============================================================
      # 21. PACKAGE UNSIGNED IPA
      # ============================================================

      - name: Package unsigned IPA
        run: |
          set -eux

          rm -rf package

          mkdir -p \
            package/Payload

          ditto \
            "$APP" \
            package/Payload/Blender.app

          cd package

          zip \
            -qry \
            -y \
            ../Blender-5.2-iPad-Cycles-OFF.ipa \
            Payload

          cd ..

          test -f \
            Blender-5.2-iPad-Cycles-OFF.ipa

          echo
          echo "========================================"
          echo "IPA CREATED"
          echo "========================================"

          ls -lh \
            Blender-5.2-iPad-Cycles-OFF.ipa

          echo
          echo "===== IPA contents ====="

          unzip -l \
            Blender-5.2-iPad-Cycles-OFF.ipa \
            | head -100

      # ============================================================
      # 22. BUILD INFORMATION
      # ============================================================

      - name: Write build information
        if: always()
        run: |
          {
            echo "Source:"
            echo "salmazov/blender-ios"
            echo

            echo "Source commit:"
            git rev-parse HEAD 2>/dev/null || true
            echo

            echo "iOS libraries:"
            git -C lib/ios_arm64 \
              rev-parse HEAD \
              2>/dev/null || true
            echo

            echo "macOS libraries:"
            git -C lib/macos_arm64 \
              rev-parse HEAD \
              2>/dev/null || true
            echo

            echo "Build options:"
            echo "WITH_CYCLES=OFF"
            echo

            echo "Compatibility fixes:"
            echo "BundleKey fmt::join"
            echo "StringRef fmt::join"
          } > build-revision.txt

          cat build-revision.txt

      # ============================================================
      # 23. UPLOAD IPA
      # ============================================================

      - name: Upload Blender IPA
        if: success()
        uses: actions/upload-artifact@v7
        with:
          name: Blender-5.2-iPad-Cycles-OFF
          path: |
            Blender-5.2-iPad-Cycles-OFF.ipa
            build-revision.txt
            compatibility.patch
          retention-days: 30

      # ============================================================
      # 24. ALWAYS UPLOAD LOGS
      # ============================================================

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: blender-ios-build-logs
          path: |
            build-revision.txt
            compatibility.patch
            source-lfs.log
            ios-lfs.log
            macos-lfs.log
            configure.log
            xcodebuild.log
            compiler-errors.txt
            fmt-errors.txt
            failed-commands.txt
            build_ios/CMakeCache.txt
          if-no-files-found: ignore
          retention-days: 7
